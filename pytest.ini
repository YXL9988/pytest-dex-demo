# pytest.ini
[pytest]
testpaths = tests
python_files = test_*.py *_test.py
addopts = -q -ra --html=reports/report.html --self-contained-html
markers =
    happy: happy case tests
    negative: error/edge case tests
    smoke: quick health checks for CI
    edge: edge cases

